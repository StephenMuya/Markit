services:
  # Web Service for the Audio System Logic
  - type: web
    name: workana-audio-system
    env: node
    plan: free
    buildCommand: npm install
    startCommand: node server.js
    envVars:
      - key: NOTION_API_KEY
        sync: false
      - key: PEOPLE_DB_ID
        sync: false
