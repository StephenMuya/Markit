services:
  # PostgreSQL Database
  - type: pserv
    name: markit-db
    plan: free
    env: docker
    region: oregon
    databases:
      - name: markit
        
  # Java Spring Boot Backend API
  - type: web
    name: markit-backend
    env: java
    plan: free
    buildCommand: cd backend && ./gradlew build -x test
    startCommand: cd backend && java -jar build/libs/markit-backend-1.0.0.jar
    envVars:
      - key: DB_HOST
        fromDatabase:
          name: markit-db
          property: host
      - key: DB_PORT
        fromDatabase:
          name: markit-db
          property: port
      - key: DB_NAME
        value: markit
      - key: DB_USER
        fromDatabase:
          name: markit-db
          property: user
      - key: DB_PASSWORD
        fromDatabase:
          name: markit-db
          property: password
          
  # Python Scraper (Cron Job - runs daily)
  - type: cron
    name: markit-scraper
    env: python
    plan: free
    schedule: "0 0 * * *"  # Daily at midnight
    buildCommand: pip install -r requirements.txt && python -m playwright install
    startCommand: cd scraping_system && python scraper.py && python extractor.py && python integration.py
    envVars:
      - key: GEMINI_API_KEY
        sync: false
      - key: DB_HOST
        fromDatabase:
          name: markit-db
          property: host
      - key: DB_PORT
        fromDatabase:
          name: markit-db
          property: port
      - key: DB_NAME
        value: markit
      - key: DB_USER
        fromDatabase:
          name: markit-db
          property: user
      - key: DB_PASSWORD
        fromDatabase:
          name: markit-db
          property: password
      - key: MAX_ARTICLES_PER_SOURCE
        value: "50"
